# Copy to apps/web/.env.local. Never commit real credentials.
# Pull development values from the linked Vercel project; use a publishable key only.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
